写接口三步完成：（核心三步曲）
                连接数据库，取数据，数据输出(返回数据）



前后端分离，接口的作用：

                       1、提供接口，获取用户输入的信息 存入数据库
                       2、调用接口，获取数据库中的内容 展示在用户页面
                       
           


fastapi:
  实时服务器；
               uvicorn main:app --reload
               uvicorn main :app --reload
       
 uvicorn main : app --reload
uvicorn main : app --reload